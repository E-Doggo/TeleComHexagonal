// import { Pool } from "pg";
// import config from "../../config";
import { DataPlanDataObject } from "./DTO";
// const pool = new Pool(config);

class DataPlanRepository {
  createDataPlanObject() {
    return "data created";
  }
}

export default DataPlanRepository;
